import { ReplaceAmPmPipe } from './replace-am-pm-pipe';

describe('ReplaceAmPmPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceAmPmPipe();
    expect(pipe).toBeTruthy();
  });
});
